services:
  - type: web
    name: pyxel-leaderboard-api
    env: python
    buildCommand: "pip install -r requirements.txt"
    startCommand: "python api.py"
    rootDir: pyxel-api
    envVars:
      - key: DATABASE_URL
        sync: false